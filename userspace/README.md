# Userspace MantleOS

Le userspace est volontairement désactivé dans la première image noyau
indépendante. Les anciens composants C conservés dans `init/`, `system/`,
`services/` et `shell/` sont des travaux préparatoires et ne sont pas embarqués
ou exécutés par la chaîne actuelle.
